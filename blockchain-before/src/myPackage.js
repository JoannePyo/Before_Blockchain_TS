/** 이코맨트를 사용해서 내 코드가 맞는지 확인 가능.
 * initializes the project
 * @param {object} config
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns {boolean}
 */
export function init(config){
    return true;
}
/**
 * Exits the program
 * @param {number} conde 
 * @returns 
 */
export function exit(conde){
    return code + 1;
}